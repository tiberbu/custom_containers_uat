name: Build Frappe Image

on:
  push:
    branches:
      - master
  workflow_dispatch:
    inputs:
      image:
        description: Image name
        type: string
      version:
        description: Image version tag
        type: string
      frappe-repo:
        description: Frappe repo URL
        type: string
      frappe-version:
        description: Frappe branch/tag
        type: string
      py-version:
        description: Python version
        type: string
      nodejs-version:
        description: NodeJS version
        type: string
      apps-json-base64:
        description: Base64 encoded string of apps.json
        type: string
      dockerfile:
        description: Dockerfile path relative to repo root
        type: string

env:
  TOKEN: ${{ secrets.TOKEN }}
  AWS_REGION: eu-west-2
  ECR_REGISTRY: ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.eu-west-2.amazonaws.com

jobs:
  build:
    runs-on: ubuntu-latest
    steps:

      - name: Checkout
        uses: actions/checkout@v4

      - name: Clean Git Cache
        run: git clean -ffdx && git reset --hard

      - name: Set environment variables from inputs or defaults
        run: |
          echo "IMAGE_NAME=${{ inputs.image || 'frappe-image' }}" >> $GITHUB_ENV
          echo "IMAGE_VERSION=${{ inputs.version || 'latest' }}" >> $GITHUB_ENV
          echo "FRAPPE_PATH=${{ inputs.frappe-repo || 'https://github.com/frappe/frappe' }}" >> $GITHUB_ENV
          echo "FRAPPE_BRANCH=${{ inputs.frappe-version || 'version-15' }}" >> $GITHUB_ENV
          echo "PYTHON_VERSION=${{ inputs.py-version || '3.12' }}" >> $GITHUB_ENV
          echo "NODE_VERSION=${{ inputs.nodejs-version || '20' }}" >> $GITHUB_ENV
          echo "APPS_JSON_BASE64=${{ inputs.apps-json-base64 || '' }}" >> $GITHUB_ENV
          echo "DOCKERFILE_PATH=${{ inputs.dockerfile || 'Dockerfile' }}" >> $GITHUB_ENV

      - name: Run Python Script to Update JSON
        env:
          TOKEN: ${{ secrets.TOKEN }}
        run: python ci/script.py

      - name: Source Build Env
        run: |
          [ -f ./ci/build.env ] && cat ./ci/build.env >> $GITHUB_ENV
          [ -f ./ci/version.txt ] && echo "VERSION=$(cat ./ci/version.txt)" >> $GITHUB_ENV
          [ -f ./ci/apps.json ] && echo "APPS_JSON_BASE64=$(base64 -w 0 ./ci/apps.json)" >> $GITHUB_ENV

      - name: Prepare temporary build context
        run: |
          mkdir -p /tmp/build-context
          cp -r "$DOCKERFILE_PATH" /tmp/build-context/Dockerfile
          cp -r ci /tmp/build-context/

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Ensure ECR repository exists
        run: |
          set -e
          if ! aws ecr describe-repositories --repository-names "$IMAGE_NAME" --region "$AWS_REGION" >/dev/null 2>&1; then
            aws ecr create-repository \
              --repository-name "$IMAGE_NAME" \
              --region "$AWS_REGION" \
              --image-scanning-configuration scanOnPush=true \
              --encryption-configuration encryptionType=AES256
            echo "Created ECR repository: $IMAGE_NAME"
          else
            echo "ECR repository already exists: $IMAGE_NAME"
          fi

      - name: Login to Amazon ECR
        run: |
          aws ecr get-login-password --region "$AWS_REGION" \
            | docker login --username AWS --password-stdin "$ECR_REGISTRY"

      - name: Build and push Docker image with Kaniko
        uses: int128/kaniko-action@v1
        with:
          push: true
          cache: false
          kaniko-args: |
            --build-arg=FRAPPE_PATH=${FRAPPE_PATH}
            --build-arg=FRAPPE_BRANCH=${FRAPPE_BRANCH}
            --build-arg=PYTHON_VERSION=${PYTHON_VERSION}
            --build-arg=NODE_VERSION=${NODE_VERSION}
            --build-arg=APPS_JSON_BASE64=${APPS_JSON_BASE64}
            --context=/tmp/build-context
            --destination=${ECR_REGISTRY}/${IMAGE_NAME}:${IMAGE_VERSION}
            --dockerfile=/tmp/build-context/Dockerfile
